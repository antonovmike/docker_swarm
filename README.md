# serde_example

The algorythm creates "config.yml" file


(Fix description)
Check image name:
```bash
sudo docker image ls
```
To start docker:
```bash
sudo docker run -it --rm IMAGE_NAME /bin/sh
```